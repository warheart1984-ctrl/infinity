# Canonical middleware is aais-middleware/ — see AAIS_MIDDLEWARE_ARCHITECTURE.md

Status: superseded pointer

Use [AAIS_MIDDLEWARE_ARCHITECTURE.md](./AAIS_MIDDLEWARE_ARCHITECTURE.md) as the doctrine + layout source of truth.

Python host: `src/constitutional_task_bus/dispatch.py` → `aais-middleware/bin/dispatch.mjs` (`runRequest`).
